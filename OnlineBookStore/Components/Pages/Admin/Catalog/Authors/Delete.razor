@page "/admin/authors/delete"
@rendermode InteractiveServer
@using Microsoft.AspNetCore.Authorization
@using OnlineBookStore.Domain
@using OnlineBookStore.Services
@attribute [Authorize(Roles = "Administrator")]

@inject AdminAuthorService Authors
@inject NavigationManager Nav

<PageTitle>Delete Author</PageTitle>

<h1 class="mb-1">Delete Author</h1>
<div class="text-muted mb-3">This action cannot be undone.</div>

@if (!string.IsNullOrWhiteSpace(error))
{
    <div class="alert alert-danger">@error</div>
}

@if (loading)
{
    <p class="text-muted">Loading...</p>
}
else if (item == null)
{
    <div class="alert alert-warning">Author not found.</div>
    <a class="btn btn-outline-secondary" href="/admin/authors">Back</a>
}
else
{
    <div class="alert alert-warning">
        Are you sure you want to delete <b>@(item.Name ?? "(No Name)")</b>?
        <div class="small mt-2 text-muted">
            Delete is blocked if books are linked to this author.
        </div>
    </div>

    <div class="d-flex gap-2">
        <button class="btn btn-danger" @onclick="DeleteAsync" disabled="@deleting">Delete</button>
        <a class="btn btn-outline-secondary" href="/admin/authors">Cancel</a>
    </div>
}

@code {
    [SupplyParameterFromQuery] public int id { get; set; }

    private bool loading = true;
    private bool deleting;
    private string? error;

    private Author? item;

    protected override async Task OnInitializedAsync()
    {
        item = await Authors.GetAuthorAsync(id);
        loading = false;
    }

    private async Task DeleteAsync()
    {
        deleting = true;
        error = null;

        try
        {
            await Authors.DeleteAsync(id);
            Nav.NavigateTo("/admin/authors");
        }
        catch (Exception ex)
        {
            error = ex.Message;
        }
        finally
        {
            deleting = false;
        }
    }
}
